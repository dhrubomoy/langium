# Ralph Progress — langium-treesitter
# Branch: ralph/langium-treesitter
# Started: 2026-05-03

## 2026-05-03 - US-006
Thread: (ralph autonomous run)
- Created `packages/langium/src/workspace/index-builder.ts` with the `IndexBuilder` interface (`build(tree: SyntaxNode, metadata: GrammarMetadata, uri: string): DocumentIndex`) and `DefaultIndexBuilder` implementation. The walker recursively visits every node in the tree, looks up `node.type` in `metadata.nodes`, and — when the matched `NodeMetadata` has a field named `name` with operator `'='` — extracts the name from `node.childForFieldName('name')` and pushes a `DeclarationInfo` keyed by the name's text into `index.declarations`. Range is taken from the *name child's* `startPosition` / `endPosition` (matches the "Source range of the name token" contract on `DeclarationInfo.range`). `uri` is parsed via `URI.parse` so the resulting `DeclarationInfo.uri` is a proper `URI` instance.
- Exposed a small `toRange(node)` helper alongside the class — US-007/US-008 will reuse it for the cross-reference walk and ERROR/MISSING diagnostics range conversion.
- Wired the service: added `IndexBuilder` to `LangiumDefaultCoreServices.workspace` in `packages/langium/src/services.ts`, registered the factory `IndexBuilder: () => new DefaultIndexBuilder()` in `packages/langium/src/default-module.ts`, and re-exported the module via `packages/langium/src/workspace/index.ts`.
- Added `packages/langium/test/workspace/index-builder.test.ts` — vitest suite with seven tests using a minimal `mockNode()` helper that fakes `type`, `text`, `startPosition`, `endPosition`, `children`, and `childForFieldName`. Coverage: single declaration; multiple declarations grouped under the same name; distinct names; nodes with no metadata entry are ignored; nodes whose metadata has no `name` field (e.g. `NumberLiteral`) produce no declaration; deeply nested declarations are still found; references/diagnostics start empty in the declarations-only walk.
- **Files changed:** `packages/langium/src/workspace/index-builder.ts` (new), `packages/langium/src/workspace/index.ts`, `packages/langium/src/services.ts`, `packages/langium/src/default-module.ts`, `packages/langium/test/workspace/index-builder.test.ts` (new), `.ralph/prd.json`, `.ralph/progress.txt`.
- **Quality:** `npx tsc -b tsconfig.build.json --force` passes; `npx tsc -p packages/langium/tsconfig.test.json --noEmit` passes; `npx eslint <changed files>` passes. Vitest still cannot run in the sandbox (rolldown native binding) so I additionally compiled the build and ran a node smoke script that imports the emitted `lib/workspace/index-builder.js`, builds the same mock trees the vitest suite uses, and asserts the same expectations — all 17 assertions PASS.
- **Learnings for future iterations:**
  - The walker is **language-agnostic by design**: it never references arithmetic-specific node names. All language-specific behavior is driven by `GrammarMetadata`. US-007 (cross-references) and US-008 (diagnostics) extend the same single-pass walk in `DefaultIndexBuilder.walk()` rather than re-walking the tree.
  - Field lookup uses `childForFieldName('name')` rather than a child by index, because tree-sitter is the single source of truth for which child holds the field — and `Definition` in the arithmetic grammar puts `name` *after* the `def` keyword anonymous child, so an index-based lookup would be off-by-one.
  - The contract is: a node has a declaration if its metadata has a field literally named `name` with operator `=`. Other "name-like" fields (e.g. `value` on `NumberLiteral`) intentionally do not produce declarations. If a future grammar wants a different name field, it should still alias to `name` in the metadata or this rule should be relaxed (out of scope for now).
  - When implementing US-007, the cross-reference walk should iterate all fields with `isRef: true` (not just one named field) and use `childForFieldName(field.name)` to find each reference child. Reuse `toRange` for `ReferenceInfo.range`.
  - When implementing US-008 (diagnostics), check `node.isError` (per web-tree-sitter docs `isError: true` for ERROR nodes whose `type === 'ERROR'`) AND `node.isMissing` for missing inserts. Both can fire in the same walk pass — push into `index.diagnostics` from inside `walk()`.
  - The DI factory takes no `services` argument because the IndexBuilder is presently stateless and metadata-driven. If future variations need `LangiumServices`, switch to `(services) => new DefaultIndexBuilder(services)` and the `LangiumDefaultCoreServices.workspace.IndexBuilder` slot is already in the right place.
---

## 2026-05-03 - US-005
Thread: (ralph autonomous run)
- Created `packages/langium/src/workspace/document-index.ts` defining `DeclarationInfo` ({ name, nodeType, range, uri }), `ReferenceInfo` ({ name, range, targetUri? }), `DocumentIndex` ({ declarations, references, diagnostics }) using `Range` and `Diagnostic` from `vscode-languageserver-types` and `URI` from `../utils/uri-utils.js`. Added a `DocumentIndexMap = Map<string, DocumentIndex>` alias so the future IndexBuilder return type has a name.
- Added `export * from './document-index.js'` to `packages/langium/src/workspace/index.ts`.
- Discovered and resolved a name conflict: `ReferenceInfo` already exists in `packages/langium/src/syntax-tree.ts` (the linker/scope-provider context with `reference`, `container`, `property` fields) and is re-exported via the top-level `index.ts`. The wildcard exports in `packages/langium/src/index.ts` therefore became ambiguous. Resolved by adding an explicit `export type { DeclarationInfo, DocumentIndex, DocumentIndexMap, ReferenceInfo } from './workspace/document-index.js'` at the end of `packages/langium/src/index.ts` so the new type wins at the public API (necessary because `DocumentIndex` itself references this `ReferenceInfo`).
- Updated three internal test files that imported the legacy `ReferenceInfo` from `'langium'` to use a relative `'../../src/syntax-tree.js'` import instead: `linker.test.ts`, `langium-parser-builder.test.ts`, `completion-provider.test.ts`. No source files in `src/` needed changes — they all import the legacy type via `../syntax-tree.js` already.
- **Files changed:** `packages/langium/src/workspace/document-index.ts` (new), `packages/langium/src/workspace/index.ts`, `packages/langium/src/index.ts`, `packages/langium/test/references/linker.test.ts`, `packages/langium/test/parser/langium-parser-builder.test.ts`, `packages/langium/test/lsp/completion-provider.test.ts`.
- **Quality:** `npx tsc -b tsconfig.build.json --force` passes; `npx tsc -p packages/langium/tsconfig.test.json --noEmit` passes; `npx eslint <changed files>` passes. Vitest still cannot run in the sandbox (rolldown binding) so I did not run the test suite, but the change is type-only.
- **Learnings for future iterations:**
  - The new `ReferenceInfo` is the public top-level export now; future LSP adapters (US-009..014) and the IndexBuilder (US-006..008) should import it via relative paths from the workspace folder (`../workspace/document-index.js`) rather than from `'langium'` to keep cross-package imports unambiguous.
  - There are now exactly four hand-written contracts the rest of the project orbits around: `GrammarMetadata` (US-002), the arithmetic `grammar.js` fixture (US-003), the arithmetic `metadata.ts` fixture (US-004), and the `DocumentIndex` shape (US-005). The IndexBuilder in US-006 has to glue these together.
  - The `targetUri` on `ReferenceInfo` is left optional intentionally — the per-document index doesn't know cross-document targets yet. Cross-document linking (if added later) can populate it during a workspace-wide pass.
---

## Codebase Patterns
- DI services: services are added to `packages/langium/src/services.ts` (LangiumDefaultCoreServices) and wired in `packages/langium/src/default-module.ts` via factory functions `(services) => new Default...(services)`.
- Generated services use the `LangiumGeneratedCoreServices` type — make new generated fields optional for backwards compatibility.
- Public exports go through barrel files: e.g. `packages/langium/src/parser/index.ts` re-exports each parser sub-module.
- File header: every source file under `packages/langium/src/` must include the Eclipse copyright header (enforced by eslint-plugin-header).
- web-tree-sitter is at v0.26.x and uses `Parser.init()` (static, returns `Promise<void>`) and `Language.load(path)` (static, returns `Promise<Language>`).
- Tests use vitest, but `npx vitest run ...` currently fails in this sandbox due to a missing rolldown native binding. This is a pre-existing environment issue, not caused by changes.
- `npm install` at the workspace root currently fails because `packages/langium-web/package.json` has a `file:../../../monaco-languageclient/packages/client` dependency that does not exist in this sandbox. Install is therefore skipped; deps that are already in node_modules continue to work for build/typecheck.
- Build/typecheck commands that work: `npx tsc --noEmit` (from a package dir) or `npx tsc -b tsconfig.build.json` (from the repo root).
- Lint command that works for changed files: `npx eslint <files>`.
- Test fixtures (non-`*.test.ts` files) under `packages/langium/test/fixtures/` are NOT picked up by vitest (`include: ['**/test/**/*.test.ts']`) and NOT linted (eslint config only matches `*.ts`/`*.tsx` under `src/` and `test/`). Tree-sitter `grammar.js` fixtures live there safely.
- Tree-sitter `grammar.js` files are JS modules that expect `grammar`, `seq`, `choice`, `field`, `prec`, `prec.left`, `prec.right`, `repeat`, `repeat1`, `optional`, `token` as globals at runtime (provided by the tree-sitter CLI sandbox). To smoke-test loading without the real CLI, eval the file in a `vm` context with stubbed globals.
- Test files under `packages/langium/test/fixtures/` are picked up by vitest as long as they end in `.test.ts`. Plain fixture data (`metadata.ts`, `grammar.js`) sit alongside `*.test.ts` runners safely.
- When vitest cannot run in the sandbox, smoke-test a pure TS data module by compiling it with `npx tsc --target ES2020 --module ESNext --moduleResolution bundler --rootDir <dir> --outDir /tmp/out <file>` and importing the emitted `.js` from a one-shot node script.
- The name `ReferenceInfo` is overloaded in the public `langium` API: the legacy linker context lives in `packages/langium/src/syntax-tree.ts`, while the new tree-sitter-driven `DocumentIndex` reference entry lives in `packages/langium/src/workspace/document-index.ts`. The new one is the public `ReferenceInfo` exported from `packages/langium/src/index.ts` (because `DocumentIndex.references` references it). Internal consumers that still need the legacy type import it via the relative `../syntax-tree.js` path. When US-027 deletes the CST/linker stack, the legacy type goes with it and the conflict resolves itself.
- TypeScript's `export *` ambiguity between two re-exported barrels can be resolved by adding an explicit `export type { Name } from './path.js'` in the same module — the explicit re-export wins over the wildcards.
- Tree-sitter `Node` API: prefer `childForFieldName('foo')` (returns the named-field child) over index-based lookup. Index-based lookup is wrong when the rule body emits anonymous keyword/punctuation children (e.g. `def NAME = EXPR;` puts the `name` field at index 1, not 0). The web-tree-sitter type is `Node`; this project aliases it to `SyntaxNode` at import sites to match the PRD vocabulary.
- LSP coordinate conversion: tree-sitter `startPosition`/`endPosition` are `{ row, column }` (zero-based); the LSP equivalent is `{ line, character }` (also zero-based). Convert with `{ line: pos.row, character: pos.column }`. The shared helper is `toRange(node)` exported from `packages/langium/src/workspace/index-builder.ts`.

## 2026-05-03 - US-002
Thread: (ralph autonomous run)
- Created `packages/langium/src/generate/grammar-metadata.ts` defining the stable `GrammarMetadata`, `NodeMetadata`, `FieldMetadata` (with `operator: '=' | '+=' | '?='` and `isRef?: boolean`), and `ReferenceMetadata` interfaces with TSDoc covering the contract for emitter/runtime consumers.
- `GrammarMetadata` carries `version`, `nodes: Record<string, NodeMetadata>`, `extras: string[]`, and optional `word`.
- Exported the four interfaces from `packages/langium/src/generate/index.ts` as `export type { ... } from './grammar-metadata.js'` so they are part of the public `langium/generate` entry point.
- **Files changed:** `packages/langium/src/generate/grammar-metadata.ts` (new), `packages/langium/src/generate/index.ts`.
- **Quality:** `npx tsc -b tsconfig.build.json` passes; `npx eslint <changed files>` passes.
- **Learnings for future iterations:**
  - `langium/generate` is a public entry point with conditional exports — anything added there is part of the API surface, so types are exported via `export type { ... }` to make the type-only nature explicit and to keep tree-shakability.
  - The PRD's runtime stories (IndexBuilder, LSP adapters) consume `GrammarMetadata` and the compiler stories (US-022/023) emit it, so this type is the contract between those two halves of the project.
  - `ReferenceMetadata` is included for forward compatibility (flattened reference list) but is not yet consumed; current cross-reference handling rides on `FieldMetadata.isRef`.
---

## 2026-05-03 - US-001
Thread: (ralph autonomous run)
- Added `web-tree-sitter@~0.26.0` to `packages/langium/package.json` dependencies.
- Created `packages/langium/src/parser/wasm-loader.ts` with the `WasmLoader` interface and `DefaultWasmLoader` class. `init()` calls `Parser.init()` then `Language.load(path)` and caches the loaded `Language`; `getLanguage()` returns the cached instance and throws if `init()` has not completed.
- Added `WasmLoader: WasmLoader` to `LangiumDefaultCoreServices.parser` in `packages/langium/src/services.ts`.
- Added an optional `parser.GrammarWasmPath?: string` field to `LangiumGeneratedCoreServices` so language-specific generated modules can supply the path; backwards compatible (existing generated modules don't break).
- Wired `WasmLoader: (services) => new DefaultWasmLoader(services.parser.GrammarWasmPath ?? '')` in `packages/langium/src/default-module.ts`.
- Re-exported the new module from `packages/langium/src/parser/index.ts`.
- **Files changed:** `packages/langium/package.json`, `packages/langium/src/parser/wasm-loader.ts` (new), `packages/langium/src/parser/index.ts`, `packages/langium/src/services.ts`, `packages/langium/src/default-module.ts`.
- **Quality:** `npx tsc -b tsconfig.build.json` passes; `npx eslint <changed files>` passes.
- **Learnings for future iterations:**
  - The path-supplied-at-construction pattern means the language-specific generated module must provide `GrammarWasmPath`. That hook is wired but no language uses it yet — US-024 will populate it in the generator.
  - `npm install` in this sandbox is broken (sibling-repo file: dep), so any deps added need to be available in `node_modules` already. `web-tree-sitter` was already present transitively, which is why the build still works.
  - Static `Parser.init()` only needs to be called once globally, but `DefaultWasmLoader.init()` is idempotent (caches `language` and `initPromise`), so multiple containers calling it concurrently is safe.
  - When adding a service to `LangiumDefaultCoreServices`, don't forget to (a) import the type in `services.ts`, (b) declare the field, (c) register the factory in `default-module.ts`, and (d) export the implementation through the parent `parser/index.ts` barrel.
---

## 2026-05-03 - US-004
Thread: (ralph autonomous run)
- Created `packages/langium/test/fixtures/arithmetic/metadata.ts` exporting `ARITHMETIC_METADATA: GrammarMetadata` (imported from `langium/generate`). Covers the 9 PRD-required nodes (Definition, AbstractDefinition, Addition, Subtraction, Multiplication, Division, Negation, NumberLiteral, NamedExpression) plus `Parenthesized` and the `Expression` supertype to fully mirror the grammar.js fixture (US-003). `version: '1.0.0'`, `word: 'ID'`, `extras: ['ML_COMMENT', 'SL_COMMENT']`. Only `NamedExpression.element` is marked `isRef: true`; all field operators are `'='` (this language has no `+=` lists or `?=` flags).
- Added `packages/langium/test/fixtures/arithmetic/metadata.test.ts` — vitest suite asserting required-node coverage, field shapes per node, sole-cross-reference invariant, and operator legality.
- **Files changed:** `packages/langium/test/fixtures/arithmetic/metadata.ts` (new), `packages/langium/test/fixtures/arithmetic/metadata.test.ts` (new).
- **Quality:** `npx tsc -b tsconfig.build.json` passes; `npx tsc -p packages/langium/tsconfig.test.json` (test project) passes; `npx eslint <changed files>` passes. Vitest cannot execute in this sandbox (pre-existing rolldown native-binding issue documented in patterns), so I additionally compiled `metadata.ts` with a standalone tsc invocation and ran a node check that asserts required-node coverage, the single `isRef`, the word/extras values, and field shapes — all PASS.
- **Learnings for future iterations:**
  - Test files under `packages/langium/test/fixtures/` ARE picked up by vitest (the `**/test/**/*.test.ts` glob is recursive). Only non-`*.test.ts` fixture files (like `grammar.js` or `metadata.ts`) are excluded from the test runner.
  - The metadata fixture intentionally contains entries for the abstract/supertype nodes (`AbstractDefinition`, `Expression`) with empty `fields: []`. Future compiler stories (US-022) need to decide whether to emit such entries; the runtime index builder (US-006) should be fine either way because its declaration walk only fires on nodes whose metadata has a `name` field.
  - Field-name contract between fixtures: `Definition` → name/expr; binary ops → left/right; `Negation` → operand; `Parenthesized` → expression; `NumberLiteral` → value; `NamedExpression` → element (the only `isRef`). Any divergence here will cascade into US-006/007/022/025 test failures.
  - When a sandbox lacks a working vitest binary, you can still smoke-test a TS module by running `npx tsc --target ES2020 --module ESNext --moduleResolution bundler --rootDir <dir> --outDir /tmp/out <files>` and importing the emitted `.js` from a one-shot node script. Keep this trick to test pure data fixtures, not anything that imports from the workspace package.
---

## 2026-05-03 - US-003
Thread: (ralph autonomous run)
- Created `packages/langium/test/fixtures/arithmetic/grammar.js` — a hand-written tree-sitter grammar for the simplified arithmetic language (Definition, AbstractDefinition supertype, Expression supertype with Addition/Subtraction/Multiplication/Division/Negation/Parenthesized/NumberLiteral/NamedExpression children, plus `Number`, `ID`, `ML_COMMENT`, `SL_COMMENT` terminals).
- Uses `grammar({ name, word: $ => $.ID, extras: $ => [/\s+/, $.ML_COMMENT, $.SL_COMMENT], supertypes: $ => [...], rules })`. Field assignments use `field('name', ...)`. Binary operators use `prec.left(N, ...)` (1 for +/-, 2 for *//), unary minus uses `prec.right(3, ...)`, and `Parenthesized` uses `prec(4, ...)` to demonstrate non-associative `prec()` and ensure paren-grouping commits early. Terminals use `token(/regex/)` with `seq(...)` for multi-part comments.
- Verified: `node --check` accepts the file as valid JS, and a `vm` stub of the tree-sitter DSL (grammar/seq/choice/field/prec/prec.left/prec.right/repeat/token) loads it cleanly and exposes all 16 rules + extras + word + supertypes.
- **Files changed:** `packages/langium/test/fixtures/arithmetic/grammar.js` (new).
- **Quality:** `npx tsc -b tsconfig.build.json` passes; the file is `.js` so it's not in the eslint or tsc include set (intentional — it's a tree-sitter source file, not project source).
- **Learnings for future iterations:**
  - Field names in this fixture are the contract that the metadata fixture (US-004) and compiler (US-022) must match: `Definition` has `name` + `expr`; binary ops have `left` + `right`; `Negation` has `operand`; `Parenthesized` has `expression`; `NumberLiteral` has `value`; `NamedExpression` has `element` (this one is `isRef: true` in metadata).
  - Used `supertypes: [$.AbstractDefinition, $.Expression]` so tree-sitter records the abstract types in `node-types.json` without producing concrete wrapper nodes — `AbstractDefinition: $ => $.Definition` is therefore an alias rather than a structural wrapper.
  - The grammar avoids any feature that the migration checker (US-019) rejects (no UnorderedGroup, no rule parameters, no negated/until tokens, no semantic predicates) — it's the canonical "post-migration" shape.
---
